import { defineComponent } from "vue";

export default defineComponent({
    name: 'ADropdownMenu',
    setup(props, { emit }) {
        return () => (
            <div>dropdown-menu</div>
        )
    }
})